'use client'

import { useMemo, useState } from 'react'
import { Activity, AlertTriangle, ArrowRight, Bell, Check, CheckCircle2, ChevronDown, ClipboardCheck, Clock3, FileCheck2, FilePlus2, Filter, HelpCircle, Home, LayoutDashboard, Menu, RefreshCw, Search, Settings, ShieldCheck, Sparkles, Upload, Users, X, XCircle } from 'lucide-react'

type Role = 'Entrepreneur' | 'Officer' | 'Admin'
type User = { name: string; email: string; role: Role }
type View = 'Overview' | 'Applications' | 'Create application' | 'Track status' | 'Inspections & Deadlines' | 'AI Recommendations' | 'Documents' | 'Schemes & Incentives' | 'Bottlenecks' | 'Analytics' | 'Notifications' | 'Settings'
type Status = 'Approved' | 'Under review' | 'Action required' | 'Submitted' | 'Rejected'

type Application = { id: string; title: string; applicant: string; department: string; status: Status; date: string; progress: number; priority: 'High' | 'Medium' | 'Low'; sla: string }
const applications: Application[] = [
  { id: 'APP-2026-0184', title: 'Factory operating licence', applicant: 'ABC Manufacturing', department: 'Industrial Safety Department', status: 'Under review', date: '18 Aug 2026', progress: 68, priority: 'High', sla: '3 days left' },
  { id: 'APP-2026-0172', title: 'Pollution consent renewal', applicant: 'Saffron Foods Pvt. Ltd.', department: 'State Pollution Control Board', status: 'Action required', date: '14 Aug 2026', progress: 42, priority: 'High', sla: '7 days left' },
  { id: 'APP-2026-0139', title: 'Power connection approval', applicant: 'ABC Manufacturing', department: 'Electricity Board', status: 'Approved', date: '05 Aug 2026', progress: 100, priority: 'Low', sla: 'Completed' },
  { id: 'APP-2026-0128', title: 'Building plan approval', applicant: 'Saffron Foods Pvt. Ltd.', department: 'Local Authority', status: 'Submitted', date: '28 Jul 2026', progress: 25, priority: 'Medium', sla: '11 days left' },
]
const approvals = [
  { name: 'Factory Operating Licence', authority: 'Industrial Safety Department', priority: 'High', reason: 'Required for manufacturing operations', time: '15 working days', docs: ['Factory layout plan', 'Safety equipment list'] },
  { name: 'Pollution Consent', authority: 'State Pollution Control Board', priority: 'High', reason: 'Industrial activity may require environmental clearance', time: '12 working days', docs: ['Emissions report', 'Water usage statement'] },
  { name: 'Fire Safety Certificate', authority: 'Fire Department', priority: 'Medium', reason: 'Required for industrial premises', time: '7 working days', docs: ['Fire plan', 'Equipment certificate'] },
  { name: 'Building Plan Approval', authority: 'Local Authority', priority: 'Medium', reason: 'Required before construction starts', time: '10 working days', docs: ['Site plan', 'Ownership proof'] },
]
const schemes = [
  { name: 'Industrial Development Incentive', dept: 'Department of Industries', match: 92, benefit: 'Capital investment support up to ₹25 lakh', status: 'Eligible', deadline: '30 Sep 2026', eligibility: 'Manufacturing units investing above ₹50 lakh' },
  { name: 'MSME Support Scheme', dept: 'MSME Development Office', match: 86, benefit: 'Interest support and advisory assistance', status: 'Potentially eligible', deadline: '15 Oct 2026', eligibility: 'Registered small and medium businesses' },
  { name: 'Green Manufacturing Grant', dept: 'Environment Department', match: 74, benefit: '40% support for cleaner equipment', status: 'Potentially eligible', deadline: '08 Sep 2026', eligibility: 'Units adopting energy-efficient processes' },
]

function StatusBadge({ status }: { status: Status | string }) { const cls = status === 'Approved' || status === 'Eligible' || status === 'On Track' ? 'status-approved' : status === 'Rejected' || status === 'SLA Breached' ? 'status-danger' : status.includes('required') || status.includes('Risk') || status === 'Approaching Deadline' ? 'status-warning' : 'status-review'; return <span className={`status-badge ${cls}`}><span className="status-dot" />{status}</span> }
function MetricCard({ label, value, helper, icon: Icon, tone = 'blue' }: { label: string; value: string; helper: string; icon: typeof Clock3; tone?: string }) { return <div className="metric-card"><div className={`metric-icon ${tone}`}><Icon size={19} /></div><div><p className="eyebrow">{label}</p><p className="metric-value">{value}</p><p className="metric-helper">{helper}</p></div></div> }
function TrendChart() { return <div className="trend-chart" aria-label="Applications trend"><div className="chart-area"><svg viewBox="0 0 600 180" preserveAspectRatio="none"><path d="M0 132 C70 116 72 124 120 105 S190 110 238 80 S300 98 360 62 S420 76 480 50 S540 47 600 28" fill="none" stroke="var(--primary)" strokeWidth="3" vectorEffect="non-scaling-stroke" /><path d="M0 155 C70 142 90 146 138 128 S202 138 244 112 S310 125 360 101 S427 109 484 83 S535 91 600 69" fill="none" stroke="#3c9293" strokeWidth="3" vectorEffect="non-scaling-stroke" /></svg><div className="chart-grid"><span>Mar</span><span>Apr</span><span>May</span><span>Jun</span><span>Jul</span><span>Aug</span></div></div></div> }

const baseNav: { label: View; icon: typeof Home; roles?: Role[] }[] = [
  { label: 'Overview', icon: LayoutDashboard }, { label: 'Applications', icon: FileCheck2 }, { label: 'Create application', icon: FilePlus2, roles: ['Entrepreneur'] }, { label: 'AI Recommendations', icon: Sparkles, roles: ['Entrepreneur'] }, { label: 'Documents', icon: Upload, roles: ['Entrepreneur'] }, { label: 'Track status', icon: Activity }, { label: 'Inspections & Deadlines', icon: ClipboardCheck }, { label: 'Schemes & Incentives', icon: Sparkles, roles: ['Entrepreneur'] }, { label: 'Bottlenecks', icon: AlertTriangle, roles: ['Officer', 'Admin'] }, { label: 'Analytics', icon: Activity, roles: ['Officer', 'Admin'] }, { label: 'Notifications', icon: Bell }, { label: 'Settings', icon: Settings },
]

function LoginScreen({ onLogin }: { onLogin: (role: Role, email: string) => void }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [role, setRole] = useState<Role>('Entrepreneur'); const [error, setError] = useState(''); return <main className="auth-screen"><section className="auth-intro"><div className="auth-brand"><div className="brand-mark"><ShieldCheck size={22} /></div><div><strong>Anumati</strong><small>Approval & compliance</small></div></div><div className="auth-message"><p className="eyebrow">One workspace for every approval</p><h1>Move your project forward with clarity.</h1><p>Find approvals, submit complete applications, and track every department from one workspace.</p><div className="auth-points"><span><CheckCircle2 size={17} />AI-powered approval suggestions</span><span><CheckCircle2 size={17} />Document checks before submission</span><span><CheckCircle2 size={17} />Clear timelines and deadline alerts</span></div></div><small className="auth-footer">Demo prototype for entrepreneurs and public departments</small></section><section className="auth-form-wrap"><form className="auth-form" onSubmit={(e) => { e.preventDefault(); if (!email || !password) return setError('Enter your email and password to continue.'); onLogin(role, email) }}><div className="mobile-auth-brand"><div className="brand-mark"><ShieldCheck size={21} /></div><strong>Anumati</strong></div><p className="eyebrow accent-text">Welcome back</p><h2>Sign in to Anumati</h2><p className="lead">Use any demo email and password to explore a role workspace.</p><label>Email address<input type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="you@company.com" /></label><label>Password<input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Enter your password" /></label><label>Continue as<select value={role} onChange={e => setRole(e.target.value as Role)}><option>Entrepreneur</option><option>Officer</option><option>Admin</option></select></label>{error && <p className="auth-error" role="alert">{error}</p>}<button className="button button-primary auth-submit" type="submit">Sign in <ArrowRight size={16} /></button><p className="auth-note">Demo only. No real account or government system is connected.</p></form></section></main> }

function Overview({ role, name, navigate, viewApp }: { role: Role; name: string; navigate: (v: View) => void; viewApp: (a: Application) => void }) { if (role === 'Officer') return <><section className="welcome-row"><div><p className="eyebrow accent-text">Industrial Safety Department</p><h1>Good morning, Rakesh.</h1><p className="lead">Your team has 24 assigned applications.</p></div><button className="button button-primary" onClick={() => navigate('Inspections & Deadlines')}><ClipboardCheck size={17} />Plan inspections</button></section><div className="metric-grid"><MetricCard label="Applications assigned" value="24" helper="8 pending review" icon={FileCheck2} /><MetricCard label="Under inspection" value="5" helper="2 scheduled today" icon={ClipboardCheck} tone="orange" /><MetricCard label="SLA at risk" value="3" helper="Needs action today" icon={AlertTriangle} tone="orange" /><MetricCard label="Approved this month" value="18" helper="91% success rate" icon={CheckCircle2} tone="green" /></div><OfficerQueue navigate={navigate} viewApp={viewApp} /></>; if (role === 'Admin') return <><section className="welcome-row"><div><p className="eyebrow accent-text">State Industry Facilitation Cell</p><h1>Good morning, Priya.</h1><p className="lead">A clear view of approval delivery across departments.</p></div><button className="button button-secondary" onClick={() => navigate('Analytics')}><Activity size={17} />Open reports</button></section><div className="metric-grid"><MetricCard label="Active applications" value="286" helper="Across 12 departments" icon={FileCheck2} /><MetricCard label="On-time delivery" value="87%" helper="Target is 90%" icon={CheckCircle2} tone="green" /><MetricCard label="At risk" value="23" helper="Needs department action" icon={AlertTriangle} tone="orange" /><MetricCard label="Avg. approval time" value="6.8d" helper="Down 1.4 days" icon={Clock3} tone="teal" /></div><BottleneckPanel /></>; return <><section className="welcome-row"><div><p className="eyebrow accent-text">Monday, 24 August 2026</p><h1>Good morning, {name}.</h1><p className="lead">Here is what needs your attention today.</p></div><button className="button button-primary" onClick={() => navigate('AI Recommendations')}><Sparkles size={17} />Get AI recommendations</button></section><div className="metric-grid"><MetricCard label="Total applications" value="12" helper="2 submitted this month" icon={FileCheck2} /><MetricCard label="In progress" value="4" helper="1 needs your action" icon={Clock3} tone="orange" /><MetricCard label="Approved" value="6" helper="50% of all applications" icon={CheckCircle2} tone="green" /><MetricCard label="Days saved" value="18" helper="Compared to last year" icon={Activity} tone="teal" /></div><div className="attention-banner"><div className="banner-icon"><AlertTriangle size={19} /></div><div><strong>One action is waiting for you</strong><p>Upload the latest emissions report for Pollution consent renewal.</p></div><button className="button button-light" onClick={() => navigate('Documents')}>Review documents <ArrowRight size={15} /></button></div><div className="content-grid"><div className="panel chart-panel"><div className="panel-header"><div><h2>Application activity</h2><p>Requests received and approved over time.</p></div></div><TrendChart /></div><div className="panel"><div className="panel-header"><div><h2>Quick workspace</h2><p>Continue your approval journey.</p></div></div><div className="quick-list"><button onClick={() => navigate('AI Recommendations')}><Sparkles size={17} /><span><strong>Find approvals with AI</strong><small>Use project details to build your checklist</small></span><ArrowRight size={15} /></button><button onClick={() => navigate('Inspections & Deadlines')}><ClipboardCheck size={17} /><span><strong>View inspections & deadlines</strong><small>3 deadlines need your attention</small></span><ArrowRight size={15} /></button><button onClick={() => navigate('Schemes & Incentives')}><Sparkles size={17} /><span><strong>Explore schemes</strong><small>3 opportunities match your profile</small></span><ArrowRight size={15} /></button></div></div></div><ApplicationTable viewApp={viewApp} /></> }

function ApplicationTable({ viewApp }: { viewApp: (a: Application) => void }) { const [q, setQ] = useState(''); const rows = applications.filter(a => `${a.title} ${a.id} ${a.department}`.toLowerCase().includes(q.toLowerCase())); return <div className="table-card"><div className="table-toolbar"><div><h3>Recent applications</h3><p>Stay on top of every approval and request.</p></div><div className="toolbar-actions"><label className="search-box"><Search size={16} /><input aria-label="Search applications" value={q} onChange={e => setQ(e.target.value)} placeholder="Search applications" /></label><button className="icon-button" aria-label="Filter applications"><Filter size={16} /></button></div></div><div className="table-scroll"><table><thead><tr><th>Application</th><th>Department</th><th>Submitted</th><th>Status</th><th>SLA</th><th /></tr></thead><tbody>{rows.map(a => <tr key={a.id}><td><button className="table-link" onClick={() => viewApp(a)}>{a.title}</button><span className="table-sub">{a.id} · {a.applicant}</span></td><td>{a.department}</td><td>{a.date}</td><td><StatusBadge status={a.status} /></td><td>{a.sla}</td><td><ArrowRight size={15} /></td></tr>)}</tbody></table></div>{!rows.length && <div className="empty-state">No applications match your search.</div>}</div> }

function OfficerQueue({ navigate, viewApp }: { navigate: (v: View) => void; viewApp: (a: Application) => void }) { return <div className="panel queue-panel"><div className="panel-header"><div><h2>Pending applications</h2><p>Review, clarify or assign the next action.</p></div><button className="text-button" onClick={() => navigate('Applications')}>Open all <ArrowRight size={14} /></button></div><div className="table-scroll"><table><thead><tr><th>Application ID</th><th>Applicant</th><th>Approval</th><th>Current stage</th><th>SLA</th><th>Risk</th><th>Action</th></tr></thead><tbody>{applications.slice(0, 3).map(a => <tr key={a.id}><td>{a.id}</td><td>{a.applicant}</td><td>{a.title}</td><td>Document review</td><td>{a.sla}</td><td><StatusBadge status={a.priority === 'High' ? 'At Risk' : 'On Track'} /></td><td><button className="text-button" onClick={() => viewApp(a)}>Review</button></td></tr>)}</tbody></table></div></div> }

function Recommendations({ navigate }: { navigate: (v: View) => void }) { const [submitted, setSubmitted] = useState(false); return <div><section className="page-heading"><p className="eyebrow accent-text">Prototype AI guidance</p><h1>AI approval recommendation</h1><p className="lead">Tell us about your project and Anumati will suggest likely approvals. This is demo logic, not a live government AI system.</p></section>{!submitted ? <div className="panel recommendation-form"><div className="form-grid"><label>Business type<select><option>Private limited company</option><option>Partnership</option><option>Proprietorship</option></select></label><label>Industry / sector<select><option>Food manufacturing</option><option>Textiles</option><option>Engineering</option><option>IT services</option></select></label><label>Project type<select><option>New manufacturing unit</option><option>Expansion</option><option>Renewal</option></select></label><label>Location / state<select><option>Maharashtra</option><option>Gujarat</option><option>Karnataka</option></select></label><label>Investment size<select><option>₹50 lakh - ₹5 crore</option><option>Below ₹50 lakh</option><option>Above ₹5 crore</option></select></label><label>Number of employees<input placeholder="e.g. 120" /></label><label>Land / building status<select><option>Land purchased, building planned</option><option>Existing building</option><option>Leased premises</option></select></label><label>Manufacturing / service type<input placeholder="e.g. packaged food" /></label></div><button className="button button-primary" onClick={() => setSubmitted(true)}><Sparkles size={16} />Generate recommendations</button></div> : <div><div className="ai-result-banner"><Sparkles size={20} /><div><strong>AI Recommended Approvals</strong><p>Based on your demo project profile: food manufacturing unit in Maharashtra.</p></div><button className="button button-outline" onClick={() => setSubmitted(false)}>Edit profile</button></div><div className="recommendation-grid">{approvals.map((a, i) => <div className="recommendation-card" key={a.name}><div className="recommendation-top"><span className="number-badge">{i + 1}</span><StatusBadge status={a.priority} /></div><h3>{a.name}</h3><p className="authority">{a.authority}</p><p>{a.reason}</p><div className="recommendation-meta"><span><Clock3 size={13} />{a.time}</span><span><FileCheck2 size={13} />{a.docs.length} documents</span></div><button className="button button-primary" onClick={() => navigate('Create application')}>Add to application <ArrowRight size={14} /></button></div>)}</div></div>}</div> }

function Documents() { const [checked, setChecked] = useState(false); return <div><section className="page-heading"><p className="eyebrow accent-text">Prototype validation</p><h1>AI document validation</h1><p className="lead">Select a document to see a simulated readiness check. No real OCR or AI processing is connected.</p></section><div className="content-grid"><div className="panel upload-panel"><div className="upload-zone"><Upload size={26} /><strong>Factory Layout Plan.pdf</strong><span>Selected demo document · 2.4 MB</span><button className="button button-outline"><Upload size={14} />Choose document</button></div><button className="button button-primary" onClick={() => setChecked(true)}><Sparkles size={16} />{checked ? 'Recheck document' : 'Run AI validation'}</button></div><div className="panel score-panel"><p className="eyebrow">Overall readiness</p><div className="score"><strong>{checked ? '82' : '—'}</strong><span>%</span></div><div className="progress"><span style={{ width: checked ? '82%' : '0%' }} /></div><p>{checked ? 'Needs correction before submission' : 'Run validation to see the score'}</p></div></div>{checked && <div className="panel validation-results"><div className="panel-header"><div><h2>AI validation result</h2><p>Factory Layout Plan.pdf · checked just now</p></div><StatusBadge status="Needs Correction" /></div><div className="validation-list"><div className="valid"><CheckCircle2 size={17} /><span><strong>Document readable</strong><small>File opens correctly and is clear.</small></span><b>Valid</b></div><div className="valid"><CheckCircle2 size={17} /><span><strong>Correct document type</strong><small>Layout plan matches the required document.</small></span><b>Valid</b></div><div className="warning"><AlertTriangle size={17} /><span><strong>Signature missing</strong><small>Add the authorised signatory signature on page 2.</small></span><b>Warning</b></div><div className="error"><XCircle size={17} /><span><strong>Date is older than allowed period</strong><small>Upload a plan issued within the last 12 months.</small></span><b>Error</b></div></div><button className="button button-primary" onClick={() => setChecked(false)}><RefreshCw size={15} />Recheck after correction</button></div>}</div> }

function Schemes() { const [filter, setFilter] = useState('All'); const list = schemes.filter(s => filter === 'All' || (filter === 'Eligible' ? s.status === 'Eligible' : filter === 'Potentially Eligible' ? s.status === 'Potentially eligible' : s.deadline.includes('08 Sep'))); return <div><section className="page-heading"><p className="eyebrow accent-text">Support matched to your profile</p><h1>Government schemes & incentives</h1><p className="lead">Explore demo recommendations based on industry, location, project size and business stage.</p></section><div className="filter-row scheme-filters">{['All', 'Eligible', 'Potentially Eligible', 'Expiring Soon'].map(f => <button key={f} className={filter === f ? 'filter-chip active' : 'filter-chip'} onClick={() => setFilter(f)}>{f}</button>)}<button className="text-button"><HelpCircle size={15} />Why am I seeing this?</button></div><div className="scheme-grid">{list.map(s => <div className="panel scheme-card" key={s.name}><div className="scheme-head"><Sparkles size={20} /><span>{s.match}% match</span></div><h2>{s.name}</h2><p className="authority">{s.dept}</p><StatusBadge status={s.status} /><p className="scheme-benefit">{s.benefit}</p><div className="scheme-details"><span>Eligibility<strong>{s.eligibility}</strong></span><span>Deadline<strong>{s.deadline}</strong></span></div><div className="scheme-actions"><button className="button button-outline">View details</button><button className="button button-primary">Check eligibility</button></div></div>)}</div></div> }

function Inspections() { return <div><section className="page-heading"><p className="eyebrow accent-text">Keep every department on time</p><h1>Inspections & deadlines</h1><p className="lead">See upcoming inspections, SLA countdowns and escalation status in one place.</p></section><div className="alert-list"><div className="deadline-alert danger"><AlertTriangle size={18} /><span><strong>Factory Licence approval has 3 days remaining before SLA breach.</strong><small>Industrial Safety Department · APP-2026-0184</small></span></div><div className="deadline-alert warning"><Clock3 size={18} /><span><strong>Pollution Consent renewal deadline is approaching.</strong><small>State Pollution Control Board · 7 days remaining</small></span></div></div><div className="panel inspection-table"><div className="panel-header"><div><h2>Inspection schedule</h2><p>Upcoming and completed department actions.</p></div><button className="button button-primary"><ClipboardCheck size={15} />Schedule inspection</button></div><div className="table-scroll"><table><thead><tr><th>Application</th><th>Inspection</th><th>Scheduled date</th><th>Department</th><th>Status</th><th>SLA</th></tr></thead><tbody><tr><td>Factory Operating Licence</td><td>Safety premises visit</td><td>12 September 2026</td><td>Industrial Safety</td><td><StatusBadge status="Scheduled" /></td><td><StatusBadge status="Approaching Deadline" /></td></tr><tr><td>Pollution Consent</td><td>Site emissions check</td><td>16 September 2026</td><td>Pollution Control Board</td><td><StatusBadge status="Pending" /></td><td><StatusBadge status="On Track" /></td></tr><tr><td>Fire Safety Certificate</td><td>Fire equipment check</td><td>05 August 2026</td><td>Fire Department</td><td><StatusBadge status="Completed" /></td><td><StatusBadge status="On Track" /></td></tr></tbody></table></div></div><div className="panel timeline-panel"><h2>Factory Licence timeline</h2><div className="timeline horizontal-timeline"><div className="timeline-item complete"><span><Check size={14} /></span><div><strong>Application submitted</strong><small>18 Aug 2026</small></div></div><div className="timeline-item complete"><span><Check size={14} /></span><div><strong>Documents verified</strong><small>22 Aug 2026</small></div></div><div className="timeline-item active"><span>3</span><div><strong>Inspection scheduled</strong><small>12 Sep 2026</small></div></div><div className="timeline-item"><span>4</span><div><strong>Department decision</strong><small>Waiting</small></div></div></div></div></div> }

function BottleneckPanel() { return <div><div className="content-grid"><div className="panel"><div className="panel-header"><div><h2>Bottleneck analysis</h2><p>Where applications are getting delayed.</p></div></div><div className="stage-bars"><div><span>Document verification</span><div className="bar"><i style={{ width: '32%' }} /></div><b>32%</b></div><div><span>Department review</span><div className="bar"><i style={{ width: '27%' }} /></div><b>27%</b></div><div><span>Inspection scheduling</span><div className="bar"><i style={{ width: '24%' }} /></div><b>24%</b></div><div><span>Final approval</span><div className="bar"><i style={{ width: '17%' }} /></div><b>17%</b></div></div></div><div className="panel"><div className="panel-header"><div><h2>Officer performance</h2><p>Current service indicators.</p></div></div><div className="health-list"><div><span>Average processing time</span><b>6.8 days</b></div><div><span>Current backlog</span><b>24 applications</b></div><div><span>SLA breaches</span><b className="health-warn">3</b></div><div><span>Response time</span><b className="health-good">1.4 days</b></div></div></div></div><div className="attention-banner bottleneck-alert"><div className="banner-icon"><AlertTriangle size={19} /></div><div><strong>Inspection scheduling is the largest source of delay.</strong><p>8 applications are affected with an average delay of 2.4 days.</p></div><button className="button button-light">View action plan <ArrowRight size={15} /></button></div></div> }

function Analytics({ role }: { role: Role }) { return <div><section className="page-heading"><p className="eyebrow accent-text">{role === 'Officer' ? 'Department performance' : 'Statewide overview'}</p><h1>Analytics & reports</h1><p className="lead">Simple insights to identify delays and improve approval delivery.</p></section><div className="metric-grid"><MetricCard label="Average processing time" value="6.8d" helper="Down 1.4 days" icon={Clock3} /><MetricCard label="SLA compliance" value="87%" helper="Target is 90%" icon={CheckCircle2} tone="green" /><MetricCard label="Applications at risk" value="23" helper="Across 12 departments" icon={AlertTriangle} tone="orange" /><MetricCard label="Completed this month" value="184" helper="18% more than July" icon={FileCheck2} tone="teal" /></div><div className="content-grid"><div className="panel chart-panel"><div className="panel-header"><div><h2>Applications received and approved</h2><p>Monthly trend across the selected period.</p></div><select className="select-control"><option>Last 6 months</option><option>This year</option></select></div><TrendChart /></div><div className="panel"><div className="panel-header"><div><h2>Status distribution</h2><p>Current application portfolio.</p></div></div><div className="donut-wrap"><div className="donut"><span>286<small>total</small></span></div><div className="donut-legend"><span><i className="d-blue" />Under review <b>43%</b></span><span><i className="d-orange" />Action required <b>8%</b></span><span><i className="d-teal" />Approved / done <b>49%</b></span></div></div></div></div><BottleneckPanel /></div> }

function ApplicationDetail({ app, onBack, role }: { app: Application; onBack: () => void; role: Role }) { const [status, setStatus] = useState<Status>(app.status); return <div><button className="back-button" onClick={onBack}>← Back</button><section className="detail-heading"><div><p className="eyebrow accent-text">{app.id}</p><h1>{app.title}</h1><p className="lead">{app.department} · Submitted {app.date}</p></div><StatusBadge status={status} /></section><div className="detail-grid"><div className="panel timeline-panel"><div className="panel-header"><div><h2>Application journey</h2><p>Every step from submission to decision.</p></div></div><div className="timeline"><div className="timeline-item complete"><span><Check size={14} /></span><div><strong>Application submitted</strong><small>{app.date} · Application received.</small></div></div><div className="timeline-item complete"><span><Check size={14} /></span><div><strong>Documents pre-validated</strong><small>All required documents passed initial checks.</small></div></div><div className="timeline-item active"><span><Clock3 size={14} /></span><div><strong>Department review</strong><small>In progress · assigned to an officer.</small></div></div><div className="timeline-item"><span>4</span><div><strong>Inspection scheduled</strong><small>Will be scheduled after review.</small></div></div><div className="timeline-item"><span>5</span><div><strong>Final decision</strong><small>Approval or feedback will appear here.</small></div></div></div></div><div className="panel"><div className="panel-header"><div><h2>{role === 'Officer' ? 'Officer actions' : 'Application actions'}</h2><p>Take the next step on this request.</p></div></div><div className="action-stack"><button className="button button-primary" onClick={() => setStatus('Approved')}><CheckCircle2 size={16} />Approve application</button><button className="button button-outline" onClick={() => setStatus('Action required')}><AlertTriangle size={16} />Request clarification</button><button className="button button-outline" onClick={() => setStatus('Rejected')}><XCircle size={16} />Reject with reason</button></div><div className="detail-note"><ShieldCheck size={16} />Demo action: the status updates instantly for presentation.</div></div><div className="panel documents-panel"><div className="panel-header"><div><h2>Required documents</h2><p>AI validation results linked to this application.</p></div><StatusBadge status="82% ready" /></div><div className="document-list"><div><FileCheck2 size={18} className="doc-check" /><span><strong>Factory layout plan</strong><small>Needs correction · signature missing</small></span><AlertTriangle size={15} /></div><div><FileCheck2 size={18} className="doc-check" /><span><strong>Ownership proof</strong><small>Verified · 22 Aug 2026</small></span><Check size={15} /></div><div><FileCheck2 size={18} className="doc-pending" /><span><strong>Emissions report</strong><small>Action required</small></span><ArrowRight size={15} /></div></div></div></div></div> }

function CreateApplication({ navigate }: { navigate: (v: View) => void }) { const [step, setStep] = useState(1); const [submitted, setSubmitted] = useState(false); if (submitted) return <div className="success-card"><div className="success-icon"><Check size={28} /></div><p className="eyebrow accent-text">Application submitted</p><h1>You are all set.</h1><p>Your application <strong>APP-2026-0191</strong> has been sent to the relevant departments. Inspection scheduling and SLA tracking are now active.</p><div className="success-actions"><button className="button button-primary" onClick={() => navigate('Track status')}>Track application <ArrowRight size={16} /></button><button className="button button-ghost" onClick={() => setSubmitted(false)}>Start another</button></div></div>; return <div><section className="page-heading"><p className="eyebrow accent-text">New request</p><h1>Start an application</h1><p className="lead">Create a complete approval request with pre-validated documents.</p></section><div className="stepper"><div className={step >= 1 ? 'step active' : 'step'}><span>1</span><b>Project details</b></div><div className={step >= 2 ? 'step active' : 'step'}><span>2</span><b>Required approvals</b></div><div className={step >= 3 ? 'step active' : 'step'}><span>3</span><b>Review & submit</b></div></div><div className="form-panel panel"><div className="form-section"><h2>{step === 1 ? 'Project details' : step === 2 ? 'Your approval checklist' : 'Review and submit'}</h2><p>{step === 1 ? 'We use these details to find the right approvals.' : step === 2 ? 'These demo approvals were suggested for your project.' : 'Check your information before sending.'}</p></div>{step === 1 && <div className="form-grid"><label>Business name <span>*</span><input placeholder="e.g. Saffron Foods Pvt. Ltd." /></label><label>Industry / sector<select><option>Food manufacturing</option><option>Textiles</option><option>Engineering</option></select></label><label>Project location<select><option>Pune, Maharashtra</option><option>Mumbai, Maharashtra</option></select></label><label>Investment size<select><option>₹50 lakh - ₹5 crore</option><option>Above ₹5 crore</option></select></label><label>Employees<input placeholder="e.g. 120" /></label><label>Business stage<select><option>New project</option><option>Expansion</option><option>Renewal</option></select></label></div>}{step === 2 && <div className="approval-checklist">{approvals.slice(0, 3).map(a => <label key={a.name}><input type="checkbox" defaultChecked /><span><strong>{a.name}</strong><small>{a.authority} · {a.time}</small></span><StatusBadge status={a.priority} /></label>)}</div>}{step === 3 && <div className="review-summary"><div><span>Business name</span><strong>Saffron Foods Pvt. Ltd.</strong></div><div><span>Project</span><strong>New food manufacturing unit</strong></div><div><span>Approvals</span><strong>3 selected · 7 documents</strong></div><div><span>Documents</span><strong>6 verified · 1 needs correction</strong></div></div>}<div className="form-actions">{step > 1 && <button className="button button-ghost" onClick={() => setStep(step - 1)}>Back</button>}<button className="button button-primary" onClick={() => step < 3 ? setStep(step + 1) : setSubmitted(true)}>{step < 3 ? 'Continue' : 'Submit application'} <ArrowRight size={15} /></button></div></div></div> }

function Notifications({ navigate }: { navigate: (v: View) => void }) { return <div><section className="page-heading"><p className="eyebrow accent-text">Stay informed</p><h1>Notifications</h1><p className="lead">Important updates from departments and Anumati.</p></section><div className="panel notice-panel"><button className="notice-item" onClick={() => navigate('Documents')}><div className="notice-icon warning"><AlertTriangle size={16} /></div><div className="notice-copy"><strong>Action required: emissions report</strong><small>Upload the latest report for Pollution Consent Renewal.</small></div><span className="notice-time">10 min ago</span><i className="unread-dot" /></button><button className="notice-item" onClick={() => navigate('Inspections & Deadlines')}><div className="notice-icon info"><ClipboardCheck size={16} /></div><div className="notice-copy"><strong>Inspection scheduled</strong><small>Factory Operating Licence inspection is set for 12 September.</small></div><span className="notice-time">2 hrs ago</span></button><button className="notice-item"><div className="notice-icon success"><CheckCircle2 size={16} /></div><div className="notice-copy"><strong>Power connection approved</strong><small>APP-2026-0139 has been approved by Electricity Board.</small></div><span className="notice-time">Yesterday</span></button></div></div> }

function AppShell({ user, onLogout }: { user: User; onLogout: () => void }) { const { role, name, email } = user; const [view, setView] = useState<View>('Overview'); const [mobileOpen, setMobileOpen] = useState(false); const [selected, setSelected] = useState<Application | null>(null); const navigate = (v: View) => { setSelected(null); setView(v); setMobileOpen(false) }; const nav = baseNav.filter(i => !i.roles || i.roles.includes(role)); return <div className="app-shell"><aside className={`sidebar ${mobileOpen ? 'open' : ''}`}><div className="brand"><div className="brand-mark"><ShieldCheck size={20} /></div><div><strong>Anumati</strong><small>Approval & compliance</small></div><button className="close-sidebar" onClick={() => setMobileOpen(false)}><X size={18} /></button></div><div className="role-switcher"><span>Workspace</span><select value={role} onChange={e => { const next = e.target.value as Role; if (next !== role) window.location.reload() }}><option>Entrepreneur</option><option>Officer</option><option>Admin</option></select><ChevronDown size={14} /></div><p className="nav-label">Workspace</p><nav className="nav-list">{nav.map(({ label, icon: Icon }) => <button key={label} className={view === label && !selected ? 'nav-item active' : 'nav-item'} onClick={() => navigate(label)}><Icon size={16} />{label}{label === 'Notifications' && <em className="alert-count">2</em>}</button>)}</nav><div className="sidebar-help"><Sparkles size={17} /><div><strong>{role === 'Entrepreneur' ? 'Need help?' : 'Team support'}</strong><small>View guidance</small></div><ArrowRight size={14} /></div><div className="profile"><div className="avatar">{name.split(' ').map(part => part[0]).join('').slice(0, 2).toUpperCase()}</div><div><strong>{name}</strong><small>{email} · {role}</small></div><button className="more-button" aria-label="Sign out" onClick={onLogout}>↗</button></div></aside><div className="main-area"><header className="topbar"><div className="breadcrumb"><button className="mobile-menu" onClick={() => setMobileOpen(true)}><Menu size={20} /></button><Home size={14} /><span>/</span><strong>{selected ? selected.title : view}</strong></div><div className="top-actions"><button className="icon-button notification-button" aria-label="Notifications" onClick={() => navigate('Notifications')}><Bell size={17} /><i /></button><div className="top-avatar">{role === 'Entrepreneur' ? 'AS' : role === 'Officer' ? 'RK' : 'PD'}</div></div></header><main className="content">{selected ? <ApplicationDetail app={selected} role={role} onBack={() => setSelected(null)} /> : view === 'Overview' ? <Overview role={role} name={name} navigate={navigate} viewApp={setSelected} /> : view === 'Applications' ? <><section className="page-heading"><p className="eyebrow accent-text">{role === 'Officer' ? 'Review queue' : 'Your workspace'}</p><h1>{role === 'Officer' ? 'Assigned applications' : 'My applications'}</h1><p className="lead">Track every submission, action and department response.</p></section><ApplicationTable viewApp={setSelected} /></> : view === 'Create application' ? <CreateApplication navigate={navigate} /> : view === 'AI Recommendations' ? <Recommendations navigate={navigate} /> : view === 'Documents' ? <Documents /> : view === 'Schemes & Incentives' ? <Schemes /> : view === 'Inspections & Deadlines' ? <Inspections /> : view === 'Bottlenecks' ? <BottleneckPanel /> : view === 'Analytics' ? <Analytics role={role} /> : view === 'Notifications' ? <Notifications navigate={navigate} /> : view === 'Track status' ? <ApplicationDetail app={applications[0]} role={role} onBack={() => navigate('Overview')} /> : <div className="panel"><h2>Settings</h2><p className="lead">Profile and notification preferences for this demo workspace.</p></div>}</main></div></div> }

export default function Page() { const [user, setUser] = useState<User | null>(null); const handleLogin = (role: Role, email: string) => { const localPart = email.split('@')[0].replace(/[._-]+/g, ' ').trim(); const name = localPart ? localPart.split(' ').map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ') : 'User'; setUser({ role, email, name }) }; return user ? <AppShell user={user} onLogout={() => setUser(null)} /> : <LoginScreen onLogin={handleLogin} /> }
